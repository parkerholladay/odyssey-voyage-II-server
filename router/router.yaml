authentication:
  router:
    jwt:
      jwks:
        - url: file:///home/parker/dev/external/odyssey-voyage-II-server/router/jwks.json
cors:
  origins:
    - http://localhost:3000
    - https://studio.apollographql.com
headers:
  all:
    request:
      - propagate:
          named: user_id
      - propagate:
          named: herp
      - propagate:
          named: foo
homepage:
  enabled: false
include_subgraph_errors:
  all: true
rhai:
  scripts: ./rhai
sandbox:
  enabled: true
supergraph:
  introspection: true
